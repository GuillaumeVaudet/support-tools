import { FilterByLevelsPipe } from './filter-by-levels.pipe';

describe('FilterByLevelsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByLevelsPipe();
    expect(pipe).toBeTruthy();
  });
});
